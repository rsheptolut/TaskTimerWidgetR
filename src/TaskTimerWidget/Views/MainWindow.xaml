<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TaskTimerWidget.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Task Timer Widget"
    Closed="Window_Closed"
    Activated="Window_Activated">

    <Grid RowSpacing="0" Background="#D0D0D0" x:Name="MainGrid"
          PointerEntered="MainGrid_PointerEntered"
          PointerExited="MainGrid_PointerExited"
          Tapped="MainGrid_Tapped">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar (Sticky Notes Style) - Draggable via SetTitleBar -->
        <Grid Grid.Row="0" Height="32" Padding="12,4,4,4" Background="#B8B8B8" x:Name="TitleBarGrid" Opacity="0" IsHitTestVisible="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Compact Mode Toggle Button (Left) -->
            <Button
                Grid.Column="0"
                x:Name="CompactModeButton"
                Content="◧"
                Width="24"
                Height="24"
                Margin="0,0,8,0"
                FontSize="14"
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                Click="CompactModeButton_Click"
                ToolTipService.ToolTip="Compact Mode (Show Active Task Only)"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#C8C8C8"/>
                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#A8A8A8"/>
                </Button.Resources>
            </Button>

            <!-- Title (Left, Hidden by Default) -->
            <TextBlock
                Grid.Column="1"
                Text=""
                FontSize="12"
                Foreground="#333333"
                VerticalAlignment="Center"
                x:Name="TitleTextBlock" />

            <!-- Minimize Button -->
            <Button
                Grid.Column="2"
                x:Name="MinimizeButton"
                Content="−"
                Width="24"
                Height="24"
                Margin="0,0,4,0"
                FontSize="14"
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                Click="MinimizeButton_Click"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#C8C8C8"/>
                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#A8A8A8"/>
                </Button.Resources>
            </Button>

            <!-- Close Button (Right, Minimal Padding) -->
            <Button
                Grid.Column="3"
                x:Name="CloseButton"
                Content="✕"
                Width="24"
                Height="24"
                FontSize="12"
                Padding="0"
                Background="Transparent"
                Foreground="#333333"
                CornerRadius="0"
                Click="CloseButton_Click" />
        </Grid>

        <!-- Task List -->
        <Grid Grid.Row="1">
            <!-- Drop Indicator Line (floating, positioned dynamically) -->
            <Border
                x:Name="DropIndicatorLine"
                Height="3"
                Background="#4CAF50"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                Margin="24,0,24,0"
                Visibility="Collapsed"
                Canvas.ZIndex="999" />

            <ScrollView x:Name="TaskScrollView" Padding="12,12,12,12">
            <StackPanel Spacing="0">
                <!-- Tasks from ItemsControl -->
                <ItemsControl
                    x:Name="TasksItemsControl"
                    ItemsSource="{Binding Tasks}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Spacing="0" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Task Card -->
                            <Border
                                Padding="10"
                                Margin="0,4"
                                CornerRadius="6"
                                BorderThickness="1"
                                BorderBrush="#1A1A1A"
                                Background="#2A2A2A"
                                PointerEntered="TaskItem_PointerEntered"
                                PointerExited="TaskItem_PointerExited"
                                Tapped="TaskItem_Tapped"
                                RightTapped="TaskItem_RightTapped"
                                PointerPressed="TaskItem_PointerPressed"
                                PointerMoved="TaskItem_PointerMoved"
                                PointerReleased="TaskItem_PointerReleased"
                                Tag="{Binding}">

                                <Grid RowSpacing="8">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <!-- Task Name + Menu Button (Top Row) -->
                                    <Grid Grid.Row="0" ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel
                                            Grid.Column="0"
                                            Orientation="Horizontal"
                                            Spacing="6"
                                            VerticalAlignment="Center">
                                            <TextBlock
                                                x:Name="DoneIndicatorTextBlock"
                                                Text="{Binding IsDoneIndicator}"
                                                FontSize="14"
                                                Foreground="#2E7D32"
                                                VerticalAlignment="Center" />
                                            <TextBlock
                                                x:Name="TaskNameTextBlock"
                                                Text="{Binding Name}"
                                                FontSize="16"
                                                FontWeight="Normal"
                                                Foreground="{Binding IsActive, Converter={StaticResource RunningTextColorConverter}}"
                                                TextWrapping="Wrap"
                                                MaxLines="3"
                                                TextTrimming="CharacterEllipsis"
                                                VerticalAlignment="Center" />
                                        </StackPanel>

                                        <Button
                                            Grid.Column="1"
                                            Content="⋮"
                                            Width="28"
                                            Height="28"
                                            FontSize="14"
                                            Padding="0"
                                            Background="#3A3A3A"
                                            Foreground="#D0D0D0"
                                            CornerRadius="4"
                                            Tag="{Binding}"
                                            Click="TaskMenuButton_Click" />
                                    </Grid>

                                    <!-- Timer Display + Percentage (Bottom Row) -->
                                    <Grid Grid.Row="1" ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Timer Display -->
                                        <TextBlock
                                            Grid.Column="0"
                                            Text="{Binding FormattedTime}"
                                            FontSize="18"
                                            FontWeight="Normal"
                                            Foreground="{Binding IsActive, Converter={StaticResource RunningTextColorConverter}}"
                                            FontFamily="ms-appx:///Assets/Fonts/TrebuchetMS.ttf#Trebuchet MS" />

                                        <!-- Percentage Display -->
                                        <TextBlock
                                            Grid.Column="1"
                                            Text="{Binding TimePercentage}"
                                            FontSize="12"
                                            FontWeight="Normal"
                                            Foreground="#999999"
                                            VerticalAlignment="Bottom" />
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- New Task Input Card (inside ScrollView, below tasks) -->
                <Border
                    x:Name="NewTaskBorder"
                    Padding="10"
                    Margin="0,4"
                    CornerRadius="6"
                    BorderThickness="1"
                    BorderBrush="#1A1A1A"
                    Background="#2A2A2A"
                    Visibility="Collapsed">
                    <Grid RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- TextBox for input -->
                        <TextBox
                            x:Name="NewTaskTextBox"
                            Grid.Row="0"
                            PlaceholderText="Task name..."
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="White"
                            Background="#2A2A2A"
                            BorderThickness="0"
                            Padding="12,8,0,8"
                            KeyDown="NewTaskTextBox_KeyDown"
                            LostFocus="NewTaskTextBox_LostFocus" />

                        <!-- Empty space where timer would be -->
                        <Grid Grid.Row="1" Height="18" />
                    </Grid>
                </Border>

                <!-- Change Time Inline Card (replaces task card at its position) -->
                <Border
                    x:Name="ChangeTimeBorder"
                    Padding="10"
                    Margin="0,4"
                    CornerRadius="6"
                    BorderThickness="1"
                    BorderBrush="#1A1A1A"
                    Background="#2A2A2A"
                    Visibility="Collapsed"
                    Tapped="ChangeTimeBorder_Tapped"
                    RightTapped="ChangeTimeBorder_RightTapped">
                    <Grid RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Task Name (read-only, same style as task card) -->
                        <TextBlock
                            x:Name="ChangeTimeTaskName"
                            Grid.Row="0"
                            FontSize="16"
                            FontWeight="Normal"
                            Foreground="White"
                            TextWrapping="Wrap"
                            MaxLines="3"
                            TextTrimming="CharacterEllipsis"
                            VerticalAlignment="Center" />

                        <!-- Time Display (same style as task card) -->
                        <TextBlock
                            x:Name="ChangeTimeDisplay"
                            Grid.Row="1"
                            FontSize="18"
                            FontWeight="Normal"
                            Foreground="White"
                            FontFamily="ms-appx:///Assets/Fonts/TrebuchetMS.ttf#Trebuchet MS" />

                        <!-- Quick Adjustment Buttons -->
                        <StackPanel
                            Grid.Row="2"
                            x:Name="ChangeTimeButtonPanel"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Spacing="3"
                            Margin="0,2,0,0" />
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollView>
        </Grid>

        <!-- Bottom Controls -->
        <StackPanel Grid.Row="2" Spacing="8" Padding="12,12,12,12">
            <!-- Total time for the day -->
            <TextBlock
                x:Name="TotalTimeTextBlock"
                FontSize="12"
                FontWeight="SemiBold"
                Foreground="#444444"
                TextAlignment="Center" />

            <!-- Day Navigation Row -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    Grid.Column="0"
                    x:Name="PreviousDayButton"
                    Content="◀"
                    Width="28"
                    Height="24"
                    FontSize="12"
                    Padding="0"
                    Background="#3A3A3A"
                    Foreground="White"
                    BorderThickness="0"
                    CornerRadius="4"
                    Click="PreviousDayButton_Click" />

                <TextBlock
                    Grid.Column="1"
                    x:Name="DayDisplayTextBlock"
                    Text="Today"
                    FontSize="11"
                    FontWeight="SemiBold"
                    Foreground="#444444"
                    TextAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="8,0" />

                <Button
                    Grid.Column="2"
                    x:Name="NextDayButton"
                    Content="▶"
                    Width="28"
                    Height="24"
                    FontSize="12"
                    Padding="0"
                    Background="#3A3A3A"
                    Foreground="White"
                    BorderThickness="0"
                    CornerRadius="4"
                    Click="NextDayButton_Click" />
            </Grid>

            <!-- Help + Add + About Button Row -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Help Button (Left side, centered between left edge and +) -->
                <Border
                    Grid.Column="0"
                    Width="32"
                    Height="32"
                    CornerRadius="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="8,0,0,0">
                    <Button
                        x:Name="HelpButton"
                        Width="32"
                        Height="32"
                        Padding="0"
                        Background="#3A3A3A"
                        Foreground="White"
                        BorderThickness="0"
                        CornerRadius="16"
                        Click="HelpButton_Click">
                        <TextBlock
                            Text="?"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="White"
                            TextAlignment="Center" />
                    </Button>
                </Border>

                <!-- Add Button (Centered) -->
                <Button
                    Grid.Column="1"
                    x:Name="AddTaskButton"
                    Content="+"
                    Width="40"
                    Height="40"
                    FontSize="20"
                    Background="#3A3A3A"
                    Foreground="White"
                    CornerRadius="4"
                    Click="AddTaskButton_Click" />

                <!-- Cleanup Button (Right side, slightly right of center) -->
                <Border
                    Grid.Column="2"
                    Width="32"
                    Height="32"
                    CornerRadius="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0">
                    <Button
                        x:Name="CleanupButton"
                        Width="32"
                        Height="32"
                        Padding="0"
                        Background="#3A3A3A"
                        Foreground="White"
                        BorderThickness="0"
                        CornerRadius="16"
                        ToolTipService.ToolTip="Clean up empty tasks for this day"
                        Click="CleanupButton_Click">
                        <TextBlock
                            Text="&#x1FA84;"
                            FontFamily="Segoe UI Emoji"
                            FontSize="15"
                            TextAlignment="Center" />
                    </Button>
                </Border>
            </Grid>

            <!-- Status Bar -->
            <TextBlock
                x:Name="StatusBar"
                FontSize="11"
                Foreground="#999999"
                TextAlignment="Center" />
        </StackPanel>

        <!-- Empty State Message (Overlay in Row 1) -->
        <StackPanel
            Grid.Row="1"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Spacing="12"
            x:Name="EmptyStatePanel">
            <TextBlock
                Text="📋"
                FontSize="48"
                TextAlignment="Center" />
            <TextBlock
                Text="No tasks yet"
                FontSize="16"
                FontWeight="SemiBold"
                Foreground="#333333"
                TextAlignment="Center" />
            <TextBlock
                Text="Click the + button to add your first task"
                FontSize="12"
                Foreground="#888888"
                TextAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
